(function () {
  'use strict';
  var original = window.ARG_BROWSER;
  if (!original) return;
  function esc(value) { return String(value == null ? '' : value).replace(/[&<>'"]/g, function (c) { return ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' })[c]; }); }
  function hit(text, query) { return esc(text).split(esc(query)).join('<em>' + esc(query) + '</em>'); }
  function results(page) {
    var query = page.query;
    var items = window.resolveChandlerSearchResults ? window.resolveChandlerSearchResults(page.webResults) : page.webResults;
    return '<section class="arg-search-page"><header class="arg-search-header"><span class="arg-search-mark"></span><div><b>Microsoft Chandler</b><small>国内版　 国际版</small></div></header><form class="arg-search-query"><span>⌕</span><input value="' + esc(query) + '" aria-label="搜索"><button type="submit">搜索</button></form><nav class="arg-search-nav">网页　　图片　　视频　　学术　　词典　　地图</nav><main class="arg-search-grid"><div class="arg-web-results"><p class="arg-result-count">' + esc(page.resultCount) + '</p>' + items.map(function (item) { return '<article><p class="arg-result-source">' + (item.ad ? '广告 · ' : '') + esc(item.source) + (item.time ? ' · ' + esc(item.time) : '') + '</p><h2>' + (item.pageId ? '<button type="button" class="arg-web-result-title is-link" data-zhao-page="' + esc(item.pageId) + '">' : '<span class="arg-web-result-title is-static">') + hit(item.title, query) + (item.pageId ? '</button>' : '</span>') + '</h2><small>' + esc(item.url) + '</small><p>' + esc(item.snippet) + '</p></article>'; }).join('') + '</div><aside><h2>深入了解 <em>' + esc(query) + '</em></h2>' + page.related.map(function (item) { return '<button type="button" data-zhao-query="' + esc(item) + '">⌕　' + esc(item) + '</button>'; }).join('') + '</aside></main></section>';
  }
  function list(items) { return '<ul>' + (items || []).map(function (item) { return '<li>' + esc(item) + '</li>'; }).join('') + '</ul>'; }
  function wiki(page) {
    var facts = page.facts || [];
    return '<section class="arg-wiki"><header class="arg-wiki-top"><b>百度百科</b><span>首页　秒懂百科　特色百科　知识专题　加入百科　百科团队</span></header><div class="arg-wiki-hero"><h1>' + esc(page.title) + '</h1><p>' + esc(page.subtitle) + '</p></div><main class="arg-wiki-main"><article><h2>人物简介</h2><p>' + esc(page.intro) + '</p><h2>人物资料</h2><table>' + facts.map(function (item, index) { return index % 2 === 0 ? '<tr><th>' + esc(item[0]) + '</th><td>' + esc(item[1]) + '</td>' + (facts[index + 1] ? '<th>' + esc(facts[index + 1][0]) + '</th><td>' + esc(facts[index + 1][1]) + '</td>' : '<th></th><td></td>') + '</tr>' : ''; }).join('') + '</table><h2>人物履历</h2>' + list(page.career) + '<h2>职务任免</h2>' + list(page.appointments) + '<h2>工作分工</h2>' + list(page.responsibilities) + '<h2>公开活动</h2>' + list(page.activities) + '</article><aside><figure class="arg-wiki-portrait" style="height:auto;display:block;margin:0;background:#fff;border:1px solid #dfe5eb;padding:5px"><img src="' + esc(page.portrait) + '" alt="赵德刚人物照片" style="display:block;width:100%;height:340px;object-fit:cover;object-position:center top"><figcaption style="padding:6px 2px 1px;color:#788695;font-size:12px">赵德刚人物照片</figcaption></figure></aside></main></section>';
  }
  function government(page) {
    function items(values) { return '<ul>' + values.map(function (value) { return '<li>' + esc(value) + '</li>'; }).join('') + '</ul>'; }
    function date(value) { var bits = value.split('-'); return '<time><b>' + esc(bits[1]) + '-' + esc(bits[2]) + '</b><small>' + esc(bits[0]) + '</small></time>'; }
    return '<section class="arg-government"><style>.arg-government{min-height:100%;background:#f4f5f6;color:#333;font:14px/1.8 "Microsoft YaHei",sans-serif}.arg-government header{background:#fff;border-top:4px solid #075da9}.arg-government .top,.arg-government nav,.arg-government main{max-width:1120px;margin:auto}.arg-government .top{height:80px;display:flex;align-items:center;justify-content:space-between}.arg-government .brand{color:#075da9;font-size:25px;font-weight:bold}.arg-government .brand small{display:block;font-size:11px;letter-spacing:1px;color:#58718a}.arg-government .search{color:#8a8a8a;border:1px solid #d6dce2;padding:5px 12px}.arg-government nav{height:43px;display:flex;align-items:center;justify-content:space-around;color:#fff}.arg-government .nav-wrap{background:#116db7}.arg-government main{background:#fff;padding:0 30px 38px;min-height:700px}.arg-government .crumb{padding:15px 0;border-bottom:1px solid #e1e5e8;color:#777}.arg-government .profile{display:grid;grid-template-columns:180px 1fr;gap:26px;padding:26px 0;border-bottom:1px solid #dce2e6}.arg-government .photo{height:240px;background:#edf1f4;border:1px solid #d5dce2;overflow:hidden}.arg-government .photo img{display:block;width:100%;height:100%;object-fit:cover;object-position:center top}.arg-government h1{margin:0;color:#145f9d;font-size:28px}.arg-government .position{margin:3px 0 16px;font-size:17px}.arg-government h2{border-left:4px solid #1773b8;border-bottom:1px solid #d9e0e6;padding:7px 0 7px 11px;margin:25px 0 12px;color:#195f99;font-size:19px}.arg-government ul{margin:0;padding-left:22px}.arg-government .updates article{display:grid;grid-template-columns:70px 1fr;gap:14px;padding:15px 0;border-bottom:1px dashed #d8dfe5}.arg-government time{color:#1d6ea9;text-align:center;border-right:1px solid #dbe3e9;line-height:1.2}.arg-government time b{display:block;font-size:16px}.arg-government time small{color:#8795a2}.arg-government .updates h3{margin:0;color:#1b5f97;font-size:16px}.arg-government .updates p{margin:4px 0 0;color:#555}.arg-government .reports li{border-bottom:1px dashed #d8dfe5;padding:8px 0;color:#27659b}@media(max-width:700px){.arg-government main{padding:0 16px}.arg-government .top{padding:0 16px}.arg-government nav{overflow:auto;justify-content:flex-start;gap:25px;padding:0 16px;white-space:nowrap}.arg-government .profile{grid-template-columns:1fr}.arg-government .photo{width:160px}}</style><header><div class="top"><div class="brand">江州市人民政府<small>JIANGZHOU MUNICIPAL PEOPLE’S GOVERNMENT</small></div><span class="search">请输入搜索关键词　⌕</span></div><div class="nav-wrap"><nav><span>首页</span><span>政务公开</span><span>政务服务</span><span>互动交流</span><span>数据开放</span><span>走进江州</span></nav></div></header><main><div class="crumb">首页　&gt;　政务公开　&gt;　市政府领导　&gt;　' + esc(page.name) + '</div><section class="profile"><div class="photo"><img src="' + esc(page.portrait) + '" alt="赵德刚人物照片"></div><div><h1>' + esc(page.name) + '</h1><p class="position">' + esc(page.position) + '</p><p>' + esc(page.intro) + '</p><h2>工作分工</h2>' + items(page.responsibilities) + '</div></section><h2>个人简历</h2>' + items(page.resume) + '<h2>工作动态</h2><section class="updates">' + page.updates.slice().reverse().map(function (item) { return '<article' + (item.futurePageId ? ' data-future-page="' + esc(item.futurePageId) + '"' : '') + '>' + date(item.date) + '<div><h3>' + esc(item.title) + '</h3><p>' + esc(item.summary) + '</p></div></article>'; }).join('') + '</section><h2>相关报道</h2><ul class="reports">' + page.reports.map(function (item) { return '<li>·　' + esc(item) + '</li>'; }).join('') + '</ul></main></section>';
  }
  function articlePage(page) { return '<article class="arg-browser-site"><div class="arg-browser-site-url">' + esc(page.site || page.url) + '</div><h1>' + esc(page.title) + '</h1><section class="arg-browser-page-block"><small>' + esc(page.publishedAt || '') + '</small>' + (page.body || []).map(function (paragraph) { return '<p>' + esc(paragraph) + '</p>'; }).join('') + '</section></article>'; }
  function simplePage(page) { return '<article class="arg-browser-site"><div class="arg-browser-site-url">' + esc(page.url) + '</div><h1>' + esc(page.title) + '</h1><p>' + esc(page.message || '该页面内容正在整理中。') + '</p></article>'; }
  window.ARG_BROWSER = { renderApp: function (container, deps) {
    original.renderApp(container, deps);
    deps.loadJson(deps.app.source + 'index.json').then(function (data) {
      var page = data.pages && data.pages['zhao-degang-search'];
      if (!page || container.dataset.zhaoSearchBound) return;
      container.dataset.zhaoSearchBound = 'true';
      var boundZhaoElements = new WeakSet();
      function show(target) { var destination = target.kind === 'results' ? page : data.pages[target.pageId], html = target.kind === 'results' ? results(page) : destination.layout === 'wiki' ? wiki(destination) : destination.layout === 'government-leader' ? government(destination) : destination.layout === 'article' ? articlePage(destination) : simplePage(destination); return window.showBrowserPage({ html: html, url: target.kind === 'results' ? 'https://www.chandler.com/search?q=' + encodeURIComponent(page.query) : destination.url || 'arg://page/' + target.pageId }); }
      window.bindBrowserZhaoView = function (root) {
        root.querySelectorAll('[data-zhao-page]').forEach(function (button) { if (boundZhaoElements.has(button)) return; boundZhaoElements.add(button); button.onclick = function () { show({ kind: 'page', pageId: button.dataset.zhaoPage }); }; });
        root.querySelectorAll('[data-zhao-query]').forEach(function (button) { if (boundZhaoElements.has(button)) return; boundZhaoElements.add(button); button.onclick = function () { var input = root.querySelector('.arg-search-query input'); if (input) input.value = button.dataset.zhaoQuery; }; });
        var form = root.querySelector('.arg-search-query'); if (form && !boundZhaoElements.has(form)) { boundZhaoElements.add(form); form.onsubmit = function (event) { event.preventDefault(); if (form.querySelector('input').value.trim() === page.query) show({ kind: 'results' }); }; }
      };
      function intercept(event) {
        var form = event.target.closest('.arg-browser-form');
        if (!form || form.querySelector('input').value.trim() !== page.query) return;
        event.preventDefault(); event.stopImmediatePropagation(); show({ kind: 'results' });
      }
      container.addEventListener('submit', intercept, true);
    });
  }};
}());

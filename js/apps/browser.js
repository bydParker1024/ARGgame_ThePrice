export async function renderApp(container, { app, loadJson, escapeHtml }) { const records=await loadJson(`${app.source}index.json`); if(!records.length){container.innerHTML='<div class="empty-app">未保存网页。<br><small>在 data/browser/index.json 登记网页快照。</small></div>';return;} const r=await loadJson(`${app.source}${records[0].file}`); container.innerHTML=`<div class="browser-address">${escapeHtml(r.url || '本地快照')}</div><article class="browser-page"><div class="eyebrow">离线网页快照</div><h1>${escapeHtml(r.title)}</h1><div class="file-preview">${escapeHtml(r.content || '')}</div></article>`; }
